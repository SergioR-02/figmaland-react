## Tecnologías usadas

- React (v18+) con TypeScript (TSX)
- Vite (dev server y build)
- CSS (archivos .css para estilos)
- ESLint (configuración incluida)
- Node.js y npm (gestor de paquetes)

Comandos rápidos (PowerShell):

```powershell
npm install
npm run dev
npm run build
```

---

